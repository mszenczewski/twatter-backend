const chai = require('chai');
const assert = chai.assert;

describe('test group', () => {
    it('test one', () => {
        assert.isTrue(true);
    });

    it('test two', () => {
        assert.isTrue(true);
    });
});
